<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.python_in_arabic">

    <!-- صلاحيات الإنترنت المطلوبة لـ Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- صلاحيات المعرض (للصور) -->
    <!-- للإصدارات القديمة -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- للإصدارات الحديثة -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- صلاحية الكاميرا -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="python_in_arabic"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- تعطيل Google Mobile Ads native provider -->
        <provider
            android:name="com.google.android.gms.ads.MobileAdsInitProvider"
            tools:node="remove" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- عشان الإضافات اللي بتستخدم text processing تشتغل -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
